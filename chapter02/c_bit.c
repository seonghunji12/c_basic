/**
 * 컴퓨터는 모든 데이터를 bit로 표현
 *   +bit는 0또는 1의 값
 *   + 8 bit =1 byte ex) 0110 1110 (1 Byte)
 *   + 1 Byte = 2^8 = 256 표현 가능
 *                  (0~256까지 표현가능)
 *   +데이터를 표현하는 Byte가 클수록 표현할 수 있는 값도 커짐
 * 
 *    정수(4byte),  실수(8byte),  문자(4byte)
 * 
 * 문자 컴파일
 * +문자 → 2진수 형태의 astic코드로 변환
 * +ex) 'A' → 65
 * +문자는 소스코드에서만 문자로 표현되고,
 * 컴퓨터 안에서는 정수와 같은 방식으로 처리
 * 
 * ASTIC 코드
 *   +사람이 사용하는 기호를 컴퓨터 안에서 표현하는 방법
 *   + http://www.ascii-codes.com/
*/